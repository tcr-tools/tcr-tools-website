---
import BackgroundParticles from './react/BackgroundParticles'
---

<section
  class="relative min-h-[80vh] flex flex-col items-start justify-center overflow-hidden pt-20 bg-code-background"
>
  <BackgroundParticles client:only="react" />

  <div class="relative z-10 space-y-6 container max-w-6xl mx-auto pointer-events-none">
    <div class="flex items-start gap-10">
      <img src="/logo.svg" alt="TCR Logo" class="h-16 mt-4.5 w-auto pointer-events-auto" />
      <div class="pointer-events-auto">
        <h1 class="text-6xl md:text-8xl font-medium leading-none tracking-tight text-primary">
          Control time,
        </h1>
        <h1 class="text-6xl md:text-8xl font-medium leading-none tracking-tight text-text-body">
          not state.
        </h1>
      </div>
    </div>

    <div class="pl-34 space-y-8 pointer-events-auto">
      <div
        class="inline-flex items-center gap-4 px-6 py-4 border border-border border-dashed bg-white/5 backdrop-blur-sm font-mono text-sm text-primary shadow-[0_0_30px_-15px_rgba(62,229,190,0.3)] hover:bg-white/10 transition-colors cursor-pointer"
      >
        <span class="opacity-70">$</span>
        <span>npm install @tcr-tools/core</span>
      </div>

      <p class="text-text-dim max-w-lg leading-relaxed text-lg font-medium">
        State is a snapshot. Bugs live in time. <br />
        Debug the past. Fork the future.
      </p>
    </div>
  </div>

  {/* Timeline Floor Visualization */}
  <div class="absolute inset-x-0 bottom-0 h-64 z-1 pointer-events-none select-none overflow-hidden">
    {/* Timeline Graphics */}
    <div class="absolute bottom-0 inset-x-0 h-full mix-blend-screen">
      <svg width="100%" height="100%" xmlns="http://www.w3.org/2000/svg" class="w-full h-full">
        <defs>
          <pattern
            id="timeline-ruler"
            x="0"
            y="0"
            width="100"
            height="100%"
            patternUnits="userSpaceOnUse"
          >
            <line x1="0" y1="0" x2="0" y2="100%" stroke="rgba(255,255,255,0.1)" stroke-width="1"
            ></line>
            <line
              x1="50"
              y1="50%"
              x2="50"
              y2="100%"
              stroke="rgba(255,255,255,0.05)"
              stroke-width="1"></line>
            <text
              x="5"
              y="95%"
              fill="rgba(255,255,255,0.3)"
              font-family="monospace"
              font-size="10"
              letter-spacing="0.1em">+ms</text
            >
          </pattern>
        </defs>

        {/* Horizontal Lanes */}
        <line
          x1="0"
          y1="40%"
          x2="100%"
          y2="40%"
          stroke="rgba(229, 203, 62, 0.1)"
          stroke-width="1"
          stroke-dasharray="4 4"></line>
        <line
          x1="0"
          y1="60%"
          x2="100%"
          y2="60%"
          stroke="rgba(62, 229, 190, 0.1)"
          stroke-width="1"
          stroke-dasharray="8 8"></line>

        {/* Repeating Ruler */}
        <rect width="100%" height="100%" fill="url(#timeline-ruler)"></rect>
      </svg>
    </div>

    {/* Gradient Overlay for blending */}
    <div
      class="absolute inset-0 bg-linear-to-t from-(--color-bg-dark)/80 via-(--color-bg-dark)/25 to-transparent z-10"
    >
    </div>
  </div>
</section>
